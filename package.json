{
  "name": "salla-mcp-server",
  "version": "1.0.0",
  "description": "Salla MCP Server — Knowledge + Action layers for Salla partners",
  "main": "dist/index.js",
  "bin": {
    "salla-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@types/dotenv": "^6.1.1",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
